# MNN 模式配置

# WSL 上 MNN 源码与构建目录
MNN_DIR="${HOME}/Projects/gpu-cpu-phone-test/MNN"
MNN_CONVERTER="${MNN_DIR}/build-host/MNNConvert"
MNN_LINUX_BUILD="${MNN_DIR}/build-linux-llm"

# 模型目录
MODEL_SRC_DIR="${HOME}/Projects/gpu-cpu-phone-test/models/qwen2-0.5b-instruct"
MNN_MODEL_DIR="${HOME}/Projects/gpu-cpu-phone-test/models/qwen2-0.5b-instruct-mnn"

# 手机 SSH 信息
PHONE_USER="u0_a111"
PHONE_IP="192.168.31.177"
PHONE_SSH_PORT="8022"
PHONE="${PHONE_USER}@${PHONE_IP}"

# 手机上模型存放路径（proot Ubuntu 路径，Termux 原生 shell 可直接访问）
PHONE_MODEL_ROOT="/data/data/com.termux/files/usr/var/lib/proot-distro/installed-rootfs/ubuntu/root/models"
PHONE_MNN_MODEL_DIR="${PHONE_MODEL_ROOT}/qwen2-0.5b-instruct-mnn"

# 默认推理参数
DEFAULT_PROMPT="hello"
DEFAULT_BACKEND="cpu"   # cpu | opencl | vulkan
